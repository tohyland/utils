" tohyland's vimrc

" Bells are annoying
set belloff=all

" Vundle setup
set nocompatible
filetype off

set rtp+=~/.vim/bundle/Vundle.vim
call vundle#begin()
Plugin 'VundleVim/Vundle.vim'

" Plugins to install

call vundle#end()
filetype plugin indent on


